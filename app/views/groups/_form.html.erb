<%= form_for @group , :html => { :class => "form-horizontal" } do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label for="name" class="col-lg-2 control-label">Nome</label>
    <div class="col-lg-10">
      <%= f.text_field :name, :class => "form-control floating-label", :id => "focusedInput"  %>
    </div>
  </div>
  <div class="form-group">
    <label for="name" class="col-lg-2 control-label">Descrição</label>
    <div class="col-lg-10">
      <%= f.text_field :description, :class => "form-control floating-label", :id => "focusedInput"  %>
    </div>
  </div>
  <div class="form-group">
    <%= f.submit "Submeter", :class => "btn btn-primary pull-right" %>
  </div>
<% end %>
